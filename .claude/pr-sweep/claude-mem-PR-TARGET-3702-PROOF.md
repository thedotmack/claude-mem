| Surface | Command run | Observed result | Base/head |
| --- | --- | --- | --- |
| Session-limit batch preservation | `bun test tests/worker/agents/response-processor.test.ts` | `ResponseProcessor > non-XML observer responses > preserves claimed work for captured Claude Code session-limit notices`; `24 pass`; `0 fail`; `confirmClaimedMessages` was not called and reset/broadcast assertions passed. | base: `await sessionManager.confirmClaimedMessages(session.sessionDbId);` in the generic non-XML branch; head: `24 pass` and the named preservation test passed. |
| Classifier boundaries | `bun test tests/sdk/output-classifier.test.ts` | `isQuotaLimitedObserverOutput > detects the captured Claude Code session and 5-hour limit notices`; `isQuotaLimitedObserverOutput > detects reversed-order exhausted session-limit wording`; `21 pass`; `0 fail`. | base: `weekly`, `subscription`, and `rate limit` are the existing anchors; head: `21 pass` and both exact exhaustion tests passed. |
| no-op and idle | `bun test tests/worker/agents/response-processor.test.ts` | `handling empty / non-XML response > clears pending work and does NOT call storeObservations on empty response`; `non-XML observer responses > warns and clears pending work when the observer returns non-XML prose`; `24 pass`; `0 fail`. | base: `await sessionManager.confirmClaimedMessages(session.sessionDbId);`; head: `24 pass` and the existing confirmation tests passed. |
| existing quota/auth | `bun test tests/sdk/output-classifier.test.ts` and `bun test tests/worker/agents/response-processor.test.ts` | `isQuotaLimitedObserverOutput > detects Claude weekly-limit prose`; `isQuotaLimitedObserverOutput > detects subscription quota prose`; `24 pass`; `0 fail`. | base: existing quota/auth branches are unchanged; head: `21 pass` and `24 pass`, with existing quota and auth coverage passing. |
| XML routing | `bun test tests/worker/agents/response-processor.test.ts` | `ResponseProcessor > parsing observations from XML response > should parse single observation from response`; `24 pass`; `0 fail`. | base: `if (!parsed.valid) {` guards the prose classifier; head: `24 pass` and XML storage tests passed. |
| Inference false-positive | `bun test tests/sdk/output-classifier.test.ts` | `isQuotaLimitedObserverOutput > does not treat approaching or documentation prose as quota prose`; `21 pass`; `0 fail`; literals `Approaching your 5-hour usage limit` and `The session limit is configurable in the documentation.` both asserted false. | base: `21 pass` was unavailable for the new test on origin/main; head: `21 pass` and both boundary assertions passed. |
| Claude Code limit prose vocabulary | `Issue artifact: bodies/claude-mem-issue-3702.md; bun test tests/sdk/output-classifier.test.ts` | `detects the captured Claude Code session and 5-hour limit notices`; `21 pass`; `0 fail`. | base: `DROP     | CLI 5-hour limit`; head: `21 pass` and captured session/5-hour notices are classified as quota prose; live provider reproduction is unreachable. |
