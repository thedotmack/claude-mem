datasets/
node_modules/
dist/
*.log
.DS_Store
.env
.env.local
*.tmp
*.temp
.install-version
.claude/settings.local.json
.claude/agents/
.claude/skills/
plugin/data/
plugin/data.backup/
package-lock.json
bun.lock
private/
datasets/

# Generated UI files (built from viewer-template.html)
src/ui/viewer.html

# Generated plugin artifacts (built from src/)
plugin/scripts/*.cjs

# Local MCP server config (for development only)
.mcp.json
.cursor/

# Prevent literal tilde directories (path validation bug artifacts)
~*/

# Prevent other malformed path directories
http*/
https*/

# BMAD workflow files (local development only)
_bmad/
_bmad-output/
.claude/commands/bmad-*.md

# Keep folder for work-in-progress documentation
keep/

# Claude-generated context files (environment-specific, never sync)
**/CLAUDE.md
CLAUDE.md

# Local utility scripts (not part of codebase)
scripts/clear-pending-queue.ts