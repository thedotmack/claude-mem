# claude-mem for Grok Bot

Install from the Grok Bot or Cursor plugin store as **claude-mem-grok-bot**. Does not install Cursor.

Grok Bot has no hooks. After install: `npx claude-mem install --ide grok-bot --provider host` for local, or set `CLAUDE_MEM_MCP_TOKEN` for remote MCP.
