// Fixture: a module that doesn't export a usable createProvider factory in
// any recognized shape — loadCustomServerGenerationProvider should warn and
// return null rather than throw.
export const somethingElse = 42;
